// videoPreview.js\nexport function initVideoHover(clubsState) {\n  // Attach hover listeners to club cards after they are rendered\n  const cards = document.querySelectorAll('.club-card');\n  cards.forEach(card => {\n    const clubId = card.id.replace('card-', '');\n    const videoSrc = `/assets/videos/${clubId}.mp4`;\n    const placeholderSrc = `/assets/videos/placeholder.mp4`;}\n    let videoEl = null;\n    card.addEventListener('mouseenter', () => {\n      videoEl = document.createElement('video');\n      videoEl.src = videoSrc;\n      videoEl.onerror = () => { videoEl.src = placeholderSrc; } ;\n      videoEl.autoplay = true;\n      videoEl.muted = true;\n      videoEl.loop = true;\n      videoEl.style.position = 'absolute';\n      videoEl.style.top = '0';\n      videoEl.style.left = '0';\n      videoEl.style.width = '100%';\n      videoEl.style.height = '100%';\n      videoEl.style.objectFit = 'cover';\n      card.appendChild(videoEl);\n    });\n    card.addEventListener('mouseleave', () => {\n      if (videoEl && videoEl.parentNode) {\n        videoEl.parentNode.removeChild(videoEl);\n        videoEl = null;\n      }\n    });\n  });\n}\n
